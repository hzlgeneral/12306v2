services:
  train-ticket:
    build: .
    image: train-ticket-processor:latest
    container_name: train-ticket-processor
    volumes:
      - ./uploads:/app/uploads
      - ./output:/app/output
      - ./发票:/app/发票
    ports:
      - "5100:5100"
    restart: unless-stopped